class SingleBuild

  def initialize(new_max, lift_name, current_user)
    @new_max = new_max
    @lift_name = lift_name
    @current_user = current_user
  end

  def add_sets
    binding.pry
  end
end
